/**
 * Shared styling constants for trip map polyline paths
 */
export const TRIP_MAP_PATH_STYLE = {
  color: "#1F6F78", // Teal (app secondary color)
  weight: 2.5,
  opacity: 0.7,
  smoothFactor: 1.0,
  lineJoin: "round" as const,
  lineCap: "round" as const,
};
